建议使用python 3.11

建议安装虚拟环境并激活：
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```
安装依赖：
```bash
pip install -r requirements.txt
```


更新代码后（例如执行 `git pull`）如有新增依赖，请重新执行：
```bash
pip install -r requirements.txt
```
